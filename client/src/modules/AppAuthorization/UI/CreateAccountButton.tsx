import { FC } from "react"
import { TouchableOpacity, Text } from "react-native"

const AppButton : FC = () => {
    return (
        <TouchableOpacity>
            <Text>Button</Text>
        </TouchableOpacity>
    )
}

export default AppButton;