import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
    },
    form: {
        width: 300,
        minHeight: 150,
    },
    input: {
        marginBottom: 10,
    },
    authorizationTitle:{
        fontSize: 27,
        marginBottom: 20,
    }
});