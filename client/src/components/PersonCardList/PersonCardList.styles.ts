import { StyleSheet } from "react-native";
import styled from "styled-components/native";

export const PersonCardFlatList = styled.FlatList`
    width: 100%;
    min-height: 600px; 
    margin-top: 20px;
`

export const styles = StyleSheet.create({
    flatList: {
        width: "100%",
        minHeight: 600,
        marginTop: 20
    }
})